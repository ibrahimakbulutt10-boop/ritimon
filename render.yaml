services:
  - type: web
    name: ritimon-fm
    env: node
    region: oregon
    plan: free
    rootDir: .  # ← Bu satır kritik!
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: STREAM_URL
        value: http://109.169.23.84:26713/stream
    # FFmpeg will be installed via Aptfile
